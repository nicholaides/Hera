{ pushTransformSource } from ../cjs.civet
{ type CompilerOptions, transpileTsToJs } from ./transpile.civet

// Add the transpile step to the CJS loader
export function initialize(compilerOptions: CompilerOptions = {})
  pushTransformSource (source: string) => transpileTsToJs(source, compilerOptions)
