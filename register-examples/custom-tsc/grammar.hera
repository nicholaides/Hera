```
// custom tsc loader settings should convert this to helper.cjs
import { helperValue }  from './helper.cts'
```

Rule
  "a" ->
    // tsc loader should transpile away the "as string"
    return helperValue as string
